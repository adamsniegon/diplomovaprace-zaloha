import React from 'react';
import Logo from '../Logo/Logo';

function Header() {
    return (
        <React.Fragment>
            <Logo/>
        </React.Fragment>
    );
  }
  
  export default Header;